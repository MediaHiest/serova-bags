services:
  db:
    image: postgres:16-alpine
    container_name: serova-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: serova_store
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d serova_store"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
